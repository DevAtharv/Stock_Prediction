Step-by-Step Guide to Set Up Your Flask Stock Prediction Project
1. Create the Project Folder Structure
Inside your main project folder (e.g., stock-predictor), create the following structure:

graphql
Copy
Edit
stock-predictor/
│
├── main.py              # Your Flask app code
├── requirements.txt     # Python dependencies
├── Procfile             # (Optional, for deployment)
├── templates/           # Folder for HTML templates
│    └── index.html      # Your main webpage template
├── static/              # (Optional) Folder for CSS, images, JS files
│    └── style.css       # (Optional) CSS styles
└── README.md            # Project description
